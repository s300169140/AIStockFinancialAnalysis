# Copy to keystore.properties and fill in — NEVER commit.
# Used by app/build.gradle.kts for release signing.

storeFile=/absolute/path/to/release.keystore
storePassword=...
keyAlias=release
keyPassword=...
